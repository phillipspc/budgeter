<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hey there</h1>
    <p>
      <% if @this_months_pending.positive? %>
        You have <%= @this_months_pending %> transactions waiting to be imported.
        You can find them <%= link_to "here", plaid_transactions_url %>.
      <% end %>
      <br>
      <% if @previous_months_pending.positive? %>
        <% intro = @this_months_pending.positive? ? "Additionally, t" : "T" %>
        <%= "#{intro}here are #{@previous_months_pending} pending transactions in" %>
        <%= link_to "last month's import", plaid_transactions_url(month: @previous_month) %>.
      <% end %>

      <br>
      You can opt out of these emails by updating your <%= link_to "settings", edit_settings_url %>.
    </p>
  </body>
</html>
