(function() {
  document.querySelector("[data-category-id='<%= @category.id %>']").
    innerHTML = "<%= j render @category, collapsed: false %>"
})()

<%= render 'application/flash_messages', format: :js %>
