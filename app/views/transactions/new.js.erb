document.querySelector("[data-container-for='modal']").
  innerHTML = "<%= j render 'modal', transaction: @transaction, category: @category, sub_category: @sub_category, redirect_url: @redirect_url %>"

document.querySelector("[data-container-for='modal'] .modal").classList.add("is-active")
